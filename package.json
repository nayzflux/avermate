{
  "name": "avermate",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "better-auth": "^0.7.5-beta.8"
  },
  "scripts": {
    "db:studio": "cd apps/api && bun run db:studio",
    "db:push": "cd apps/api && bun run db:push",
    "db:dev": "cd apps/api && bun run db:dev",
    "api:dev": "cd apps/api && bun run dev",
    "client:dev": "cd apps/client && bun run dev"
  }
}
